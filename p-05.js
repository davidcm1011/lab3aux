// Convertido la función sortFruits a arrow function
const sortFruits = fruits => fruits.sort();

// Convertido la función getElementInTheMiddle a arrow function
const getElementInTheMiddle = list => list[Math.floor(list.length / 2)];

// Convertido la función adminLogin a arrow function
const adminLogin = user => user === "ADMIN" || user === "DEV";
